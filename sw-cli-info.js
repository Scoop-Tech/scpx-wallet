// Distributed under AGPLv3 license: see /LICENSE for terms. Copyright 2019-2021 Dominic Morris.

const utilsWallet = require('./utils')
const log = require('./sw-cli-log')

module.exports = {

    show: (appWorker, store, p) => {
        log.cmd('TODO')

        return new Promise((resolve) => {
            
            resolve({ ok: true })
        })
    },

}